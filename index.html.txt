<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>NEXUS CENTRAL | Master Portal</title>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@700&family=Roboto:wght@300;400;900&display=swap" rel="stylesheet">
    <style>
        :root {
            --c-bg-main: radial-gradient(circle at center top, #11192b 0%, #000000 100%);
            --c-bg-panel: rgba(12, 18, 30, 0.95);
            --c-purple: #a855f7;
            --c-cyan: #00e5ff;
            --c-pink: #ff33bb;
            --f-title: 'Orbitron', sans-serif;
            --f-main: 'Roboto', sans-serif;
        }

        body, html { margin: 0; padding: 0; height: 100%; background: var(--c-bg-main); font-family: var(--f-main); color: #eee; overflow: hidden; }

        /* Estructura Unificada */
        .nexus-layout { display: flex; height: 100vh; }

        /* Barra Lateral (Capa Alta de gestión) */
        nav {
            width: 300px; background: rgba(15, 22, 36, 0.8); border-right: 1px solid rgba(168, 85, 247, 0.3);
            display: flex; flex-direction: column; padding: 20px;
        }

        .header-bar {
            padding: 15px; margin-bottom: 30px;
            background: linear-gradient(90deg, rgba(168, 85, 247, 0.2) 0%, rgba(0,0,0,0) 100%);
            border-left: 5px solid var(--c-purple); border-bottom: 1px solid rgba(168, 85, 247, 0.3);
        }
        .header-title { font-family: var(--f-title); font-size: 1.2rem; letter-spacing: 2px; }

        /* Capa de Unificación de Centros */
        .nav-section { margin-bottom: 25px; }
        .section-label { font-size: 10px; color: var(--c-purple); font-weight: 900; text-transform: uppercase; letter-spacing: 2px; margin-bottom: 15px; display: block; }
        
        .nav-item {
            padding: 15px; border-radius: 12px; background: rgba(255,255,255,0.03);
            border: 1px solid rgba(255,255,255,0.1); cursor: pointer; transition: 0.3s;
            display: flex; align-items: center; gap: 15px; margin-bottom: 10px;
        }
        .nav-item:hover { border-color: var(--c-cyan); box-shadow: 0 0 15px rgba(0, 229, 255, 0.2); }
        .nav-item.active { border-color: var(--c-pink); background: rgba(255, 51, 187, 0.05); }

        /* Contenido Central */
        main { flex: 1; padding: 30px; overflow-y: auto; }
        
        /* Grid estilo IoT replicado */
        .nexus-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(350px, 1fr)); gap: 20px; }
        .nexus-card { background: var(--c-bg-panel); border: 1px solid rgba(255,255,255,0.1); border-radius: 12px; padding: 20px; position: relative; overflow: hidden; }
        
        .card-label { font-size: 13px; color: var(--c-purple); font-weight: 900; text-transform: uppercase; display: flex; align-items: center; gap: 10px; margin-bottom: 20px; }
        .card-label::after { content: ""; flex: 1; height: 1px; background: rgba(168, 85, 247, 0.4); }

        /* Animación de escaneo replicada del IoT */
        .glow-line {
            position: absolute; top: 0; left: -100%; width: 50%; height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.1), transparent);
            animation: scan 4s infinite;
        }
        @keyframes scan { 0% { left: -100%; } 100% { left: 150%; } }
    </style>
</head>
<body>
    <div class="nexus-layout">
        <nav>
            <div class="header-bar">
                <span class="header-title">NEXUS MASTER</span>
            </div>
            
            <div class="nav-section">
                <span class="section-label">CENTROS ACTIVOS</span>
                <div class="nav-item active">
                    <span style="font-size: 20px;">🏫</span>
                    <div>
                        <div style="font-weight: 900; font-size: 14px;">SALESIANOS REPELEGA</div>
                        <div style="font-size: 10px; color: var(--c-cyan);">IOT CONECTADO - UR3e</div>
                    </div>
                </div>
                <div class="nav-item">
                    <span style="font-size: 20px;">🏫</span>
                    <div>
                        <div style="font-weight: 900; font-size: 14px;">URNIETA</div>
                        <div style="font-size: 10px; color: #666;">OFFLINE</div>
                    </div>
                </div>
            </div>
        </nav>

        <main>
            <div class="nexus-grid">
                <div class="nexus-card">
                    <div class="glow-line"></div>
                    <div class="card-label">ESTADO GLOBAL DEL NODO</div>
                    <div style="display: flex; justify-content: space-between; align-items: center;">
                        <div>
                            <span style="display:block; font-size:10px; color:#94a3b8;">LATENCIA MQTT</span>
                            <span style="font-family: var(--f-title); font-size: 24px; color: var(--c-cyan);">24ms</span>
                        </div>
                        <div style="text-align: right;">
                            <span style="display:block; font-size:10px; color:#94a3b8;">PAQUETES/SEC</span>
                            <span style="font-family: var(--f-title); font-size: 24px; color: var(--c-pink);">60Hz</span>
                        </div>
                    </div>
                </div>

                <div class="nexus-card" style="grid-column: span 2; min-height: 400px; border-style: dashed; border-color: #333; display: flex; align-items: center; justify-content: center;">
                    <p style="color: #444; font-family: var(--f-title);">VISOR DE TELEMETRÍA EN TIEMPO REAL</p>
                </div>
            </div>
        </main>
    </div>
</body>
</html>